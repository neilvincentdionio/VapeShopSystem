<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Choose a new delivery date. The customer and admin will be notified."
        android:textColor="#1E40AF"
        android:textSize="12sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="New delivery date"
        android:textColor="#111827"
        android:textSize="13sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/rider_reschedule_pick_date"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="6dp"
        android:background="@drawable/bg_checkout_input"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center_vertical"
        android:paddingHorizontal="10dp"
        android:text="Select date"
        android:textColor="#111827"
        android:textSize="13sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="Reason (optional)"
        android:textColor="#111827"
        android:textSize="13sp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/rider_reschedule_reason"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:background="@drawable/bg_checkout_input"
        android:gravity="top|start"
        android:hint="e.g. Customer not available, weather delay..."
        android:inputType="textMultiLine|textCapSentences"
        android:minLines="2"
        android:maxLength="255"
        android:padding="10dp"
        android:textSize="14sp" />
</LinearLayout>
